/**
 * The <i>MapMouseEventType</i> enum. TODO: Document.
 *
 * @author Jab
 */
enum MapMouseEventType {
  DOWN = 'down',
  UP = 'up',
  DRAG = 'drag',
  HOVER = 'hover',
  ENTER = 'enter',
  EXIT = 'exit',
  WHEEL_UP = 'wheel_up',
  WHEEL_DOWN = 'wheel_down'
}

export default MapMouseEventType;
