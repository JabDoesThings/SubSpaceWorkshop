/**
 * The <i>TileEntry</i> interface. TODO: Document.
 *
 * @author Jab
 */
export default interface TileChunkEntry {
  id: number;
  x: number;
  y: number;
  texture: number;
  current: number[];
}
